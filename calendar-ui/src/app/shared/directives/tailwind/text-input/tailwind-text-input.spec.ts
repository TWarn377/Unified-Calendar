import { TailwindTextInput } from './tailwind-text-input';

describe('TailwindTextInput', () => {
  it('should create an instance', () => {
    const directive = new TailwindTextInput();
    expect(directive).toBeTruthy();
  });
});
