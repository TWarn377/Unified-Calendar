import { TailwindLabel } from './tailwind-label';

describe('TailwindLabel', () => {
  it('should create an instance', () => {
    const directive = new TailwindLabel();
    expect(directive).toBeTruthy();
  });
});
