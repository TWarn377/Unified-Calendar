import { TailwindCheckbox } from './tailwind-checkbox';

describe('TailwindCheckbox', () => {
  it('should create an instance', () => {
    const directive = new TailwindCheckbox();
    expect(directive).toBeTruthy();
  });
});
